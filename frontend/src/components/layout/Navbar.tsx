/**
 * Navbar — top navigation bar placeholder.
 * Full implementation with links and user menu in Milestone 2.
 */
const Navbar = () => (
  <nav aria-label="Main navigation">
    <span>TrackWise</span>
  </nav>
);

export default Navbar;
