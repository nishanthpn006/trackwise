package com.trackwise.entity;

/**
 * RecurrenceFrequency — Supported recurrence intervals for automated/recurring transactions.
 */
public enum RecurrenceFrequency {
    DAILY,
    WEEKLY,
    MONTHLY,
    YEARLY
}
